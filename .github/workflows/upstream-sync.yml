name: Sync wxpush Upstream

on:
  schedule:
    - cron: '0 20 * * *'
  workflow_dispatch:
    inputs:
      skip_notifications:
        description: 'Skip notifications'
        required: false
        default: false
        type: boolean

env:
  UPSTREAM_REPO: https://github.com/frankiejun/wxpush.git
  SYNC_BRANCH_PREFIX: 'sync-upstream'

jobs:
  sync-upstream:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config --global user.name 'GitHub Actions Bot'
          git config --global user.email 'actions@users.noreply.github.com'

      - name: Add upstream remote
        run: |
          git remote add upstream ${{ env.UPSTREAM_REPO }}

      - name: Fetch upstream changes
        run: |
          git fetch upstream
          git fetch upstream --tags

      - name: Check for real changes
        id: check_changes
        run: |
          echo "Checking for real changes between main and upstream/main..."
          
          LOCAL_COMMIT=$(git rev-parse main)
          UPSTREAM_COMMIT=$(git rev-parse upstream/main)
          
          echo "Local commit: $LOCAL_COMMIT"
          echo "Upstream commit: $UPSTREAM_COMMIT"
          
          if [ "$LOCAL_COMMIT" = "$UPSTREAM_COMMIT" ]; then
            echo "changes_detected=false" >> $GITHUB_OUTPUT
            echo "status=up_to_date" >> $GITHUB_OUTPUT
            echo "✅ Repository is up to date"
          else
            if ! git diff --quiet main upstream/main; then
              echo "changes_detected=true" >> $GITHUB_OUTPUT
              echo "status=changes_found" >> $GITHUB_OUTPUT
              echo "🔄 Real file changes detected"
            else
              echo "changes_detected=false" >> $GITHUB_OUTPUT
              echo "status=history_only" >> $GITHUB_OUTPUT
              echo "✅ No file changes, only commit history differences"
            fi
          fi

      - name: Reset and force update
        if: steps.check_changes.outputs.changes_detected == 'true'
        id: merge
        run: |
          SYNC_BRANCH="${{ env.SYNC_BRANCH_PREFIX }}-$(date +%s)"
          echo "branch_name=$SYNC_BRANCH" >> $GITHUB_OUTPUT
          
          echo "Creating sync branch: $SYNC_BRANCH"
          git checkout -b $SYNC_BRANCH
          git reset --hard upstream/main
          
          if ! git diff --quiet main..$SYNC_BRANCH; then
            echo "merge_status=success" >> $GITHUB_OUTPUT
            echo "🔄 Real changes confirmed"
          else
            echo "merge_status=no_changes" >> $GITHUB_OUTPUT
            echo "⚠️ No actual changes after reset"
          fi

      - name: Push sync branch
        if: steps.check_changes.outputs.changes_detected == 'true' && steps.merge.outputs.merge_status == 'success'
        run: |
          git push --force origin ${{ steps.merge.outputs.branch_name }}
          echo "✅ Branch pushed: ${{ steps.merge.outputs.branch_name }}"

      - name: Create Pull Request
        if: steps.check_changes.outputs.changes_detected == 'true' && steps.merge.outputs.merge_status == 'success'
        uses: actions/github-script@v7
        id: create_pr
        with:
          script: |
            const syncBranch = '${{ steps.merge.outputs.branch_name }}';
            const upstreamRepo = '${{ env.UPSTREAM_REPO }}';
            
            try {
              const pr = await github.rest.pulls.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title: 'Sync upstream changes from wxpush',
                head: syncBranch,
                base: 'main',
                body: '## Upstream Sync\n\nAutomated sync from: ' + upstreamRepo + '\n\nSync branch: ' + syncBranch + '\nSync time: ' + new Date().toISOString()
              });
              
              console.log('PR created: ' + pr.data.html_url);
              
              await github.rest.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: pr.data.number,
                labels: ['auto-sync', 'upstream']
              });
              
              core.setOutput('pr_url', pr.data.html_url);
              
            } catch (error) {
              console.error('Failed to create PR: ' + error);
              core.setFailed('PR creation failed');
            }

      - name: Send Telegram Notification
        if: always() && github.event.inputs.skip_notifications != 'true'
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: |
            Upstream Sync Status: ${{ job.status }}
            Repository: ${{ github.repository }}
            Changes: ${{ steps.check_changes.outputs.changes_detected }}
            Status: ${{ steps.check_changes.outputs.status }}
            ${{ steps.create_pr.outputs.pr_url && format('PR: {0}', steps.create_pr.outputs.pr_url) || '' }}
            Time: $(date -u +"%Y-%m-%d %H:%M UTC")

      - name: Send Discord Notification
        if: always() && github.event.inputs.skip_notifications != 'true' && secrets.DISCORD_WEBHOOK
        uses: sarisia/actions-status-discord@v1
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          title: "Upstream Sync - ${{ job.status }}"
          description: "Repository: ${{ github.repository }}\nStatus: ${{ steps.check_changes.outputs.status }}\nChanges: ${{ steps.check_changes.outputs.changes_detected }}\n${{ steps.create_pr.outputs.pr_url && format('PR: %s', steps.create_pr.outputs.pr_url) || '' }}"
          color: ${{ job.status == 'success' && '00ff00' : 'ff0000' }}

      - name: Cleanup
        if: always()
        run: |
          git remote remove upstream || true
